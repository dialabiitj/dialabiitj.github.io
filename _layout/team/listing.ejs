```{=html}
<div class="grid">
    <% for (const item of items) { %>
        <div class="g-col-12 g-col-sm-6 g-col-md-4 g-col-lg-3">
            <div class="our-team">
              <div class="picture">
                <a href="<%= item.url %>">
                  <img class="img-fluid" src="<%= item.image %>" alt="<%= item.title %>" title="<%= item.title %>">
                </a>
                </div>
              
              <div class="team-content">
                <a href="<%- item.path %>"><h3 class="name"><%= item.title %></h3></a>
                <h4 class="title"><%= item.position %></h4>
              </div>
              <% if (item.links) { %>
                <ul class="social">
                    <% for (const link of item.links) { %>
                      <li><a href="<%- link.url %>" title="<%- link.name %>" aria-hidden="true"><i class="<%- link.icon %>"></i></a></li>
                    <% } %>
                  </ul>
              <% } %>
            </div>
          </div>
    <% } %>
</div>
```